<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="linkdroid.login.test"
      android:versionCode="1"
      android:versionName="1.0">
      
  <uses-sdk android:minSdkVersion="8" />
 <uses-permission android:name="android.permission.INTERNET" />
    <application android:icon="@drawable/ic_launcher" android:label="@string/app_name">
        
        <activity android:name=".LoginActivity"
                  android:label="@string/app_name"
                  android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
 <activity android:name=".Scanner"
     android:screenOrientation="portrait"
            android:label="@string/qr">
        </activity>
 <activity android:name=".Remote_help"
     android:screenOrientation="portrait"
            android:label="@string/remote">
        </activity>
        <activity android:name="linkdroid.login.test.Warning"
            android:label="@string/warning_title"
            android:theme="@android:style/Theme.Dialog"> 
            <!-- The theme is that it generates a window on top of the menu instead of a new  -->
        </activity>
    </application>
</manifest>

